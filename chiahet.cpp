#include <bits/stdc++.h>
#include <stack>
using namespace std;
string add(string n, string k)
{
    int i,j=0;
    string a;
    if (n.size() >= k.size())
    {
        for ( i < n.size(); i >= 0; i++)
        {
            
        }
    }
}
int main()
{
    int n, k;
    cin >> n >> k;
}