{
    "tasks": [
      {
        "type": "shell",
        "label": "g++.exe build active file",
        "command": "C:\\mingw32\\bin\\g++.exe",
        "args": [
          "-g",
          "${file}",
          "-o",
          "${fileDirname}\\${fileBasenameNoExtension}.exe"
        ],
        "options": {
          "cwd": "C:\\mingw32\\bin\\g++.exe"
        }
      },
      {
        "type": "shell",
        "label": "g++ build & run active file",
        "command": "C:\\mingw32\\bin\\g++.exe",
        "args": [
          "${file}",
          "-o",
          "${fileDirname}\\${fileBasenameNoExtension}.exe"
        ],
        "options": {
          "cwd": "C:\\mingw32\\bin"
        }
      }
    ],
    "version":"2.0.0"
  }