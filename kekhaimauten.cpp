#include<bits/stdc++.h>
using namespace std;
int main(){
    ios_base::sync_with_stdio(false);cin.tie(0);cout.tie(0);
    string a;
    getline(cin,a);
    if(a.length()>30) cout<<"KHONG";
    else cout<<"CO";
}